# lilisp - Light LISP
A small LISP interpreter

## Usage
`./lil` - Enter the REPL

`./lil [file path]` - Execute a file

The `tests` directory contains a couple of files that can be executed.

## Building
`g++ -o lil -O3 *.cpp`

; Recursively finds the nth fibonacci number
(define (fib n)
    (cond
      ((= n 1) 1)
      ((= n 2) 1)
      (t (+
	  (fib (- n 1))
	  (fib (- n 2))))))

(list
 (quote (The tenth fibonacci number is))
 (fib 10))
